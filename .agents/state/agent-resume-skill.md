# Agent log — building the resume skill + repo-intact guard

Working file for the agent building `.claude/skills/session-resume/` and
`check:repo-intact`. Append-only. If this session dies, read the last entry
and continue from "NEXT".

## 2026-09-04 — entry 1 (start)

READ: CLAUDE.md, AGENTS.md, HANDOFF.md (§2, §8j, §14), .agents/rules/00 and 70,
.claude/skills/supabase-resume/SKILL.md, mobile/scripts/supabase-queue.mjs,
agent-docs-check.mjs, sync-agent-docs.mjs, root + mobile package.json,
.github/workflows/ listing.

FOUND (pre-existing, not caused by me): `npm run check:agent-docs` FAILS on
this branch already —

    FAIL  .agents/rules/63-anki-import.md points at mobile/src/lib/apkgFormat.ts, which does not exist  (x2)
    FAIL  .agents/rules/63-anki-import.md points at mobile/src/lib/apkgExport.ts, which does not exist

The real files are `src/lib/apkgFormat.ts` / `src/lib/apkgExport.ts` (shared,
repo root), which is what CLAUDE.md says. The rules file has `mobile/`
wrongly prefixed. Fixing the pointer is in my lane (.agents/rules/) and is
required for "make check:agent-docs pass".

KEY FACTS about the mechanism I must extend:
- `CLAUDE.md`'s rules table and `.agents/rules/30-reference.md`'s skill index
  are GENERATED by `mobile/scripts/sync-agent-docs.mjs`. Do not hand-edit;
  run `npm run sync:agent-docs`.
- `AGENTS.md`'s rules table is hand-written (only 00/10/20 listed) and
  AGENTS.md is 12053 bytes against a 12000-CHAR cap — check counts chars of
  the utf8 string, so I must MEASURE before adding a row.
- House style for a check script: long `//` header naming the bug it exists
  for, `  FAIL  <reason>` lines, a summary line, exit 1; `OK  ...` on success.

PLAN:
 1. mobile/scripts/resume-status.mjs   (derive + print + --write snapshot)
 2. .agents/state/session-state.json   (generated snapshot, stamped)
 3. .agents/state/resume-notes.md      (hand-written half, stamped entries)
 4. mobile/scripts/repo-intact-check.mjs + check:repo-intact
 5. .claude/skills/session-resume/SKILL.md
 6. .agents/rules/05-resume.md, row in AGENTS.md, sync:agent-docs for CLAUDE.md
 7. npm scripts in mobile/ and root
 8. workflow steps (android-debug/internal/release, webpack.yml)
 9. .agents/REPO-PROTECTION.md (honest human half)
10. evaluate SessionStart hook -> .claude/settings.json or written-down why not

NEXT: write mobile/scripts/resume-status.mjs.

## 2026-09-04 — entry 2

DONE:
- `mobile/scripts/resume-status.mjs` — written, runs, verified output (git,
  ages, blockers with probes, supabase queue, .agents/queue, 3 integrity
  checks, next actions). Flags: --json --write --no-checks. Exits 0 always
  (it is a report; the checks are what fail).
- `mobile/scripts/repo-intact-check.mjs` — written, runs, 39 paths OK.
  FILES / DIRS(with floors) / WORKFLOWS / BYTES floors + queue JSON parses +
  staged-deletion detection under guarded prefixes.
- `.agents/state/blocked.json` (6 blockers, each with owner + offline probe)
- `.agents/state/resume-notes.md` (format doc + first entry)
- FIXED pre-existing check:agent-docs failure (3 dead pointers in
  63-anki-import.md: mobile/src/lib/apkg{Format,Export}.ts -> src/lib/...).
  `npm run check:agent-docs` now prints OK.

CONSTRAINT DISCOVERED: AGENTS.md is 11999 chars against the 12000 cap —
ONE character of headroom. Adding the resume row + section requires trimming
~590 chars of existing prose first, without dropping any rule. Trims planned:
the IDE blockquote, and six wording tightenings. Measure with:
  node -e "console.log(require('fs').readFileSync('AGENTS.md','utf8').length)"

NEXT: AGENTS.md edit -> .agents/rules/05-resume.md -> .claude/skills/session-resume/
-> npm scripts (mobile + root) -> sync:agent-docs -> workflows -> REPO-PROTECTION.md
-> SessionStart hook -> guard fire-drill.

## 2026-09-04 — entry 3

DONE since entry 2:
- AGENTS.md: added the `05-resume.md` table row, a "A dead session leaves
  nothing but the repo" section, and `check:repo-intact` in the verify block.
  Paid for it by trimming ~530 chars of wording across ~25 edits — NO RULE WAS
  DROPPED, only wording. 11999 -> 11981 chars (19 under the 12000 cap).
- `.agents/rules/05-resume.md` written (frontmatter description present, which
  sync-agent-docs reads for CLAUDE.md's generated table).
- `.claude/skills/session-resume/SKILL.md` written.
- npm scripts: mobile/ gained check:repo-intact, resume, resume:write;
  root gained resume, resume:write, check:repo-intact (root because a fresh
  clone's first instinct is the root — same reason dev:mobile is there).
  Both verified by running them.

NEXT: sync:agent-docs (regenerates CLAUDE.md's rules table + 30-reference.md's
skill index) -> .claude/hooks/session-start.sh + .claude/settings.json ->
.agents/REPO-PROTECTION.md -> workflow steps -> guard fire-drill -> tsc/eslint.

## 2026-09-04 — entry 4

DONE since entry 3:
- `npm run sync:agent-docs` run: CLAUDE.md's rules table and
  .agents/rules/30-reference.md's skill index both gained the new rows
  (both are GENERATED blocks — never hand-edit them).
- `.claude/hooks/session-start.sh` + `.claude/settings.json` (SessionStart
  hook). Read-only, offline, exits 0 always. Tested twice: normal run (134
  lines), and with resume-status.mjs moved away -> prints a named error and
  still exits 0.
  NOTE FOR THE REPORT: this is a harness-configuration change. Flag it to the
  owner explicitly; deleting .claude/settings.json disables it.
- `.agents/REPO-PROTECTION.md` — the human half, with click paths, and the
  written-down reason there is NO mirror workflow (it would need a secret
  nobody has set = another silently-skipping step, §8f/§14.4).
- check:repo-intact wired into android-debug/internal/release (in the Checks
  block, just before check:agent-docs) and webpack.yml (before `npm ci`,
  because it needs no deps). All four YAML files re-parsed OK.

NEXT: the fire drill (delete a load-bearing file, watch the guard fail, restore),
then check:agent-docs + tsc + eslint, then the final report.

## 2026-09-04 — entry 5 (finished)

ALL DONE. Fire drills all fired (exit 1) and all restored (exit 0):
 1. rules file deleted            -> "is GONE — the contract that stops two agents..."
 2. 26 bank .ts files removed     -> caught by the BYTES floor (203296 < 400000), NOT by
                                     the topics file-count floor (68 recursive files ->
                                     42 > 20). The two floors are complementary; keep both.
 3. staged deletion (git rm --cached .agents/rules/50-notes.md) -> caught, exit 1
 4. supabase-pending.json made unparseable -> caught, exit 1
 5. android-release.yml deleted   -> caught, exit 1
 6. resume-notes entry back-dated to 2026-08-01 -> printed with
    "<-- OLDER THAN HEAD. Read it as history" — the anti-staleness flag works.

Green: check:agent-docs (25 rules files, all paths resolve — was FAILING before
this session), check:repo-intact (39 paths), check:supabase-queue, tsc --noEmit,
eslint . --quiet, eslint on both new scripts.

Working tree left dirty for the parent session to commit. NOT committed, NOT pushed.
Also appended HANDOFF.md §15.

FLAG IN THE REPORT: .claude/settings.json is new (SessionStart hook). It is a
harness-configuration change and the owner should be told explicitly.

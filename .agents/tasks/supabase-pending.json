{
  "jobs": [
    {
      "id": "sb-nocache-deploy",
      "kind": "deploy_function",
      "summary": "Deploy generate-flashcards so it honours noCache (live is v9, repo has it)",
      "file": "supabase/functions/generate-flashcards/index.ts",
      "why": "A personal AI deck must not be written into the shared cache. The suffixed personalDeckKey() already protects the shared row on v9, so nothing is broken meanwhile \u2014 this removes the stray cache row a personal generation leaves behind. Do NOT remove the key suffix when deploying this.",
      "verify": "get_edge_function generate-flashcards \u2192 version > 9, and its source contains `noCache`. Then: generate a personal deck from a chapter's + menu and confirm no new row appears in `flashcards` with a `#own-` deck_key.",
      "status": "done",
      "queuedAt": "2026-08-27T02:47:14.984Z",
      "appliedAt": "2026-08-27T08:12:23.806Z"
    },
    {
      "id": "sb-drop-note-images-bucket",
      "kind": "storage",
      "summary": "Delete the empty `note-images` storage bucket from the Supabase dashboard",
      "why": "It was created for a synced-notes design that was reversed the same hour: study notes are on-device only. Its four RLS policies are already dropped and it holds zero objects, so nothing can reach it and nothing is at risk \u2014 but an empty private bucket is a thing every future reader has to reason about before concluding it is dead. Briefly declined on 2026-09-01 and then asked for again once the owner knew what it was; that is the whole reason a decision belongs in this file rather than in a chat message. SQL cannot remove it: storage.protect_delete() raises 42501 on any direct delete from the storage tables, and that guard should be respected rather than disabled. supabase-tasks.yml now has a step that does it through the Storage API, which refuses a non-empty bucket of its own accord.",
      "verify": "select count(*) from storage.buckets where id='note-images' returns 0.",
      "status": "pending",
      "queuedAt": "2026-08-27T08:29:39.524Z"
    }
  ]
}

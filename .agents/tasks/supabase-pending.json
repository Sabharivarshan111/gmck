{
  "jobs": [
    {
      "id": "sb-nocache-deploy",
      "kind": "deploy_function",
      "summary": "Deploy generate-flashcards so it honours noCache (live is v9, repo has it)",
      "file": "supabase/functions/generate-flashcards/index.ts",
      "why": "A personal AI deck must not be written into the shared cache. The suffixed personalDeckKey() already protects the shared row on v9, so nothing is broken meanwhile \u2014 this removes the stray cache row a personal generation leaves behind. Do NOT remove the key suffix when deploying this.",
      "verify": "get_edge_function generate-flashcards \u2192 version > 9, and its source contains `noCache`. Then: generate a personal deck from a chapter's + menu and confirm no new row appears in `flashcards` with a `#own-` deck_key.",
      "status": "done",
      "queuedAt": "2026-08-27T02:47:14.984Z",
      "appliedAt": "2026-08-27T08:12:23.806Z"
    },
    {
      "id": "sb-drop-note-images-bucket",
      "kind": "storage",
      "summary": "Delete the empty `note-images` storage bucket from the Supabase dashboard",
      "why": "DECLINED BY THE APP'S OWNER, 2026-09-01 \u2014 do not delete this bucket. Kept deliberately; the entry stays here so the next session does not re-queue it. Original reasoning, still accurate: it was created for a synced-notes design that was reversed the same hour, since study notes are on-device only. Re-verified on 2026-09-01 and it is completely inert: private, zero objects, and zero RLS policies referencing it, so nothing can read or write it. The only cost of keeping it is that a future reader of the dashboard has to work out whether notes sync \u2014 which this note now answers, so that cost is paid. Nothing is at risk either way.",
      "verify": "Nothing to apply. If you are checking it is still harmless: select public, (select count(*) from storage.objects o where o.bucket_id='note-images') as files from storage.buckets where id='note-images';  -- expect false, 0",
      "status": "declined",
      "queuedAt": "2026-08-27T08:29:39.524Z",
      "declinedAt": "2026-09-01T03:40:00.000Z"
    }
  ]
}

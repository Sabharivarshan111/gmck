name: ORBIT Simulator Integrity

on:
  push:
    branches: [main]
    paths:
      - "src/simulator/**"
      - "src/pages/Simulator.tsx"
      - "public/models/**"
      - "scripts/simulator-organ-check.mjs"\n      - "scripts/simulator-model-asset-check.mjs"\n      - "scripts/simulator-model-asset-check.mjs"
      - ".github/workflows/simulator-integrity.yml"
  pull_request:
    paths:
      - "src/simulator/**"
      - "src/pages/Simulator.tsx"
      - "public/models/**"
      - "scripts/simulator-organ-check.mjs"
      - ".github/workflows/simulator-integrity.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  simulator-integrity:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Install dependencies
        run: npm ci --legacy-peer-deps --ignore-scripts
      - name: Organ resolver regression suite
        run: npm run check:simulator
      - name: Production build
        run: npm run build
      - name: Deploy-size guard
        run: npm run check:deploy

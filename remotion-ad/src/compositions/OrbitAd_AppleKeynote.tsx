import React from 'react';
import { Master25ShotTimeline } from '../components/Master25ShotTimeline';

export const OrbitAd_AppleKeynote: React.FC = () => {
  return (
    <Master25ShotTimeline
      themeKey="apple_keynote"
      themeType="apple"
      themeColor="#38bdf8"
    />
  );
};
